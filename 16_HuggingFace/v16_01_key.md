<!-- 실제 토큰은 프로젝트 루트의 .env 에 HF_TOKEN 으로 보관 (git 추적 제외) -->
<!-- 토큰 발급: https://huggingface.co/settings/tokens -->
<!-- 코드에서 읽는 법:
     from dotenv import load_dotenv
     token = os.getenv("HF_TOKEN")
-->
<!-- 주의: HTML 주석(<!-- -->)은 화면에 안 보일 뿐, 파일에는 그대로 남음.
     주석 처리했다고 토큰이 숨겨지는 게 아님. git에 올리면 그대로 공개됨. -->
