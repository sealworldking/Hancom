1. 외부(external) — <link>로 별도 .css 파일을 연결하는 방식. 여러 페이지에서 재사용 가능해서 실무에서 가장 권장됨.
2. 내부(internal) — <head> 안에 <style> 태그로 직접 작성. 그 페이지 안에서만 적용됨.
3. 인라인(inline) — 태그에 style="..." 속성으로 바로 작성. 그 요소 하나에만 적용됨.